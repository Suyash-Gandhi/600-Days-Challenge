

# 20) `typeof` and `instanceof` — Full Explanation

Understanding `typeof` and `instanceof` is essential in JavaScript because they help developers identify the type of data they are working with. Both are type-checking mechanisms, but they operate differently, have different limitations, and are used in different contexts.

---

# ✅ 1. `typeof` Operator

### 📌 What is `typeof`?

 `typeof` is a unary operator that returns a string describing the primitive type of a value.
 Best suited for detecting primitive data types.

### 📌 Syntax

```js
typeof value
```

### 📌 What does `typeof` return?

| Value                 | typeof Result |
| --------------------- | ------------- |
| Number                | `"number"`    |
| String                | `"string"`    |
| Boolean               | `"boolean"`   |
| Undefined             | `"undefined"` |
| BigInt                | `"bigint"`    |
| Symbol                | `"symbol"`    |
| Function              | `"function"`  |
| Object / Array / null | `"object"`    |

---

# 🎯 2. Important Details About `typeof`

### ✔ Works best for primitives

It accurately identifies:

 number
 string
 boolean
 undefined
 symbol
 bigint

### ✔ Special case: `null`

 `typeof null` returns `"object"`
 This is a historical JavaScript bug
 It cannot be fixed because it would break the web

### ✔ Functions

 `typeof functionName === "function"`
 Only functions get this special return value

### ✔ Arrays

 `typeof []` returns `"object"`
 Cannot distinguish between object and array

To check arrays, use:

```js
Array.isArray(arr)
```

---

# 💡 When to use `typeof`

 When checking primitive types
 When confirming a variable is not `undefined`
 When verifying if something is a function

---

# 🔥 Example

```js
console.log(typeof 10);           // "number"
console.log(typeof "Hello");      // "string"
console.log(typeof null);         // "object" (JS bug)
console.log(typeof {});           // "object"
console.log(typeof []);           // "object"
console.log(typeof (() => {}));   // "function"
```

---

# ---------------------------------------

# 3. `instanceof` Operator

### 📌 What is `instanceof`?

 `instanceof` checks prototype inheritance.
 It answers:
  Does obj inherit from Constructor.prototype?

### 📌 Syntax

```js
object instanceof Constructor
```

### 📌 What does it return?

 Returns a Boolean: `true` or `false`.

---

# 🎯 4. How `instanceof` Works

### ✔ Prototype-chain based

It checks whether the constructor’s prototype appears in the object’s prototype chain.

Example:

```js
arr instanceof Array     // true
arr instanceof Object    // true (because Array → Object)
```

### ✔ Works only for non-primitive objects

 Objects
 Arrays
 Functions
 Instances created using classes or constructors

### ❌ Cannot check primitive types

```js
10 instanceof Number  // false (because 10 is primitive)
new Number(10) instanceof Number  // true
```

---

# 🔥 Example

```js
class Car {}
const c = new Car();

console.log(c instanceof Car);     // true
console.log(c instanceof Object);  // true
console.log(Car instanceof Function); // true
```

---

# ---------------------------------------

# 🚀 5. `typeof` vs `instanceof` — Comparison

| Feature                     | `typeof`           | `instanceof`    |
| --------------------------- | ------------------ | --------------- |
| Checks                      | Primitive types    | Prototype chain |
| Output                      | String             | Boolean         |
| Works on primitives         | YES                | NO              |
| Works on objects            | Partially          | YES             |
| Can detect functions        | YES                | YES, indirectly |
| Detect arrays               | NO                 | YES             |
| Detect user-defined classes | NO                 | YES             |
| Detect null                 | Wrong → `"object"` | ❌ Cannot        |

---

# ---------------------------------------

# 🧠 6. When to Use What (Interview Guide)

### Use `typeof` when:

 You need to check primitive types
 You want to verify something is a function
 You want a quick check without touching prototypes

### Use `instanceof` when:

 You are working with classes
 You want to validate objects created via constructors
 You need to check inheritance relationships
 You want to differentiate `Object` vs `Array` vs custom types

---

