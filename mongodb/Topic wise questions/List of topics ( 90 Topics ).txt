

#  MongoDB Interview Master List (90 Topics)

---

### 1. Core Fundamentals (Junior Level)

1. What MongoDB is & NoSQL vs SQL databases
2. Collections vs Documents
3. BSON vs JSON
4. Installing & connecting to MongoDB locally
5. Connecting via MongoDB Compass
6. MongoDB Atlas basics (cloud-hosted MongoDB)
7. Basic CRUD operations: insert, find, update, delete
8. Difference between `insertOne` vs `insertMany`
9. Difference between `findOne` vs `find`
10. Updating documents (`updateOne`, `updateMany`)
11. Deleting documents (`deleteOne`, `deleteMany`)
12. Data types in MongoDB (ObjectId, Date, Array, etc.)
13. Working with ObjectId (`_id`)
14. Embedded documents vs references
15. Projections (`find` with specific fields)

---

### 2. Queries & Operators

16. Query operators (`$eq`, `$ne`, `$in`, `$nin`)
17. Comparison operators (`$gt`, `$gte`, `$lt`, `$lte`)
18. Logical operators (`$and`, `$or`, `$not`, `$nor`)
19. Element operators (`$exists`, `$type`)
20. Array operators (`$size`, `$all`, `$elemMatch`)
21. Regex queries (`$regex`)
22. Text search (`$text`, `$search`)
23. Sorting (`sort`)
24. Pagination (`skip`, `limit`)
25. Cursor operations

---

### 3. Indexing

26. What indexes are & why they're needed
27. Creating indexes (`createIndex`)
28. Single field vs compound indexes
29. Unique indexes
30. Sparse indexes
31. Partial indexes
32. Multikey indexes (arrays)
33. Text indexes
34. Geospatial indexes
35. Index performance analysis (`explain()`)

---

### 4. Aggregation Framework

36. What aggregation pipelines are
37. `$match` stage
38. `$project` stage
39. `$group` stage
40. `$sort` stage
41. `$limit` & `$skip`
42. `$lookup` (joins across collections)
43. `$unwind` (flatten arrays)
44. `$addFields` / `$set`
45. `$facet` (multi-pipeline)
46. `$bucket` & `$bucketAuto`
47. `$out` & `$merge` (writing results)
48. Aggregation vs mapReduce
49. Aggregation performance considerations

---

### 5. Data Modeling

50. Embedding vs referencing
51. One-to-one relationships
52. One-to-many relationships
53. Many-to-many relationships
54. Polymorphic schemas
55. Schema validation rules (`$jsonSchema`)
56. Designing for atomic operations
57. Trade-offs of normalization vs denormalization
58. Document size limits (16MB)

---

### 6. Transactions & Consistency

59. ACID properties in MongoDB
60. Single-document atomic operations
61. Multi-document transactions
62. Using sessions (`startSession`)
63. Commit & abort transactions
64. Write concerns (w, j, majority)
65. Read concerns (local, majority, linearizable)
66. Read preferences (primary, secondary, nearest)

---

### 7. Security

67. Authentication (SCRAM, x.509 certs)
68. Role-based access control (RBAC)
69. Built-in roles (`read`, `readWrite`, `dbAdmin`)
70. User management (`createUser`, `grantRolesToUser`)
71. Field-level access control
72. Encryption at rest
73. TLS/SSL for connections
74. Auditing in MongoDB

---

### 8. Performance & Scaling

75. Profiling queries (`db.currentOp`, profiler)
76. Capped collections
77. Time-to-Live (TTL) indexes
78. Sharding basics (shard keys, balancing)
79. Replication basics (primary, secondary, arbiter)
80. Replica set failover process
81. Read/write splitting in replica sets
82. Write performance tuning (batch inserts, bulk writes)
83. Avoiding schema pitfalls (large arrays, deep nesting)
84. Query optimization strategies

---

### 9. Advanced & Senior-Level Concepts

85. Change streams (real-time data notifications)
86. Triggers in MongoDB Atlas
87. Aggregation pipelines inside transactions
88. Distributed transactions across shards
89. Working with GridFS for large files
90. MongoDB Ops Manager & Monitoring

